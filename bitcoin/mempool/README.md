# mempool.space (lokální explorer)

Nasazuj až když **electrs** indexuje a Knots RPC odpovídá. UI defaultně LAN (`8080`).

```bash
cp .env.example .env
docker compose up -d
```

Nepublikuj na Cloudflare Tunnel bez autentizace.
